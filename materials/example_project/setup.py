from setuptools import setup, find_packages

setup(
    name="spc",
    version="3.1415.92653589793",
    packages=find_packages(),
    install_requires=[
        "matplotlib",
        "numpy"
    ],
)